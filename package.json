{
  "name": "ai-berkshire-reports",
  "private": true,
  "type": "module",
  "scripts": {
    "sync": "node src/scripts/sync-reports.mjs",
    "dev": "npm run sync && vitepress dev docs",
    "build": "npm run sync && vitepress build docs",
    "preview": "vitepress preview docs"
  },
  "devDependencies": {
    "vitepress": "^1.6.3"
  }
}
